---
version: 5
defaults:
  datadir: hiera
  data_hash: yaml_data
hierarchy:
  - name: "OS family"
    path: "os/%{facts.os.family}.yaml"

  - name: "Other directories"
    paths: 
      - "common.yaml"
